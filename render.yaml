services:
  - type: web
    name: sonickraft-backend
    env: python
    buildCommand: |
      pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: NIXPACKS_PKGS
        value: ffmpeg
      - key: PYTHON_VERSION
        value: 3.10.0
